apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kafka-azure-managed-default-disk
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: default
  resources:
    requests:
      storage: 5Gi
