apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prometheus-standalone-pvc
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 10Gi
